"""Eval harness — runs the gold set against the live Bedrock pipeline.

Filled in by Module 07. For now this is a no-op stub so the weekly GitHub
Action has something to invoke.
"""

# TODO (Module 07): load gold_set/*.json, run each prompt through the
# orchestrator, score with a judge model, write a timestamped report to
# results/.


def main() -> None:
    # TODO: replace with real eval loop.
    print("evals/harness.py: stub — no evals defined yet.")


if __name__ == "__main__":
    main()
