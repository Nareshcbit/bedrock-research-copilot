# Python 3.12. Pin exact versions from your lockfile before first deploy.
# TODO: replace == TODO with the resolved versions you tested against.

boto3==1.43.16
langchain-aws==1.5.0
pytest==9.0.3
requests==2.34.2
python-dotenv==1.2.2

# Suggested companions (uncomment + pin when needed):
# pydantic==TODO
# tenacity==TODO   # retries / backoff for Bedrock rate limits
